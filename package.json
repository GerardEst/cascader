{
  "name": "@gerardest/cascader",
  "version": "1.3.1",
  "description": "Very basic and tiny masonry. Easy, responsive and resizeable blocks.",
  "main": "build/cascader-min.js",
  "scripts": {
    "build": "esbuild src/cascader.js --drop:console --bundle --minify --sourcemap --format=esm --outfile=build/cascader-min.js",
    "watch": "esbuild src/cascader.js --bundle --minify --watch --sourcemap --format=esm --outfile=build/cascader-min.js"
  },
  "keywords": [
    "masonry",
    "responsive",
    "resize"
  ],
  "author": "gerardest",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.7"
  }
}
