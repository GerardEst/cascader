{
  "name": "@gerardest/cascader",
  "version": "1.1.1",
  "description": "Veeeery basic masonry",
  "main": "cascader.js",
  "scripts": {
    "build": "esbuild cascader.js --drop:console --bundle --minify --sourcemap --format=esm --outfile=cascader-min.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.47"
  }
}
